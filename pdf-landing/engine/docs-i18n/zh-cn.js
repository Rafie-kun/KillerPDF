/* Generated Simplified Chinese translation of the engine documentation. English remains in the pages as the fallback. */
(function(){window.KPDF_ENGINE_DOCS_L10N={
"lang":"zh-cn",
"ui":{"devguide":"开发者指南","previous":"上一页","next":"下一页","engineSource":"引擎源代码","loadErrorTitle":"无法加载本指南。","loadErrorBody":"请通过 KillerPDF 网站或本地 Web 服务器打开此页面。","nav":{"help":"帮助","technical":"技术","engine":"引擎","about":"关于"},"groups":{"Engine":"引擎","Start here":"从这里开始","Work with documents":"处理文档","Standards and security":"标准与安全","Reference":"参考"},"titles":{"overview":"概述","getting-started":"快速上手","reading-pdfs":"读取 PDF","creating-pdfs":"创建 PDF","editing-pdfs":"编辑 PDF","validation":"校验","signing-encryption":"签名与加密","api":"API 指南","architecture":"架构"}},
"docs":{"overview":"# The KillerPDF.Engine\n\nThe KillerPDF.Engine 是一个独立的、不含 UI 的 .NET 10 库，用于读取、校验、创建、结构化编辑、签名、加密和写出 PDF 文件。它为应用程序提供现代化的 PDF 2.0、PDF/A 和 PDF/UA 基础，而不必绑定 KillerPDF 的桌面界面。\n\n## 开始构建\n\n从 [NuGet.org](https://www.nuget.org/packages/KillerPdf.Engine) 安装软件包：\n\n```powershell\ndotnet add package KillerPdf.Engine\n```\n\n[快速上手](getting-started.html)\n\n如果你已经清楚要做什么，可直接使用侧边栏中的任务指南。\n\n## 引擎覆盖的范围\n\n- PDF 语法、对象、流、交叉引用表、交叉引用流、对象流、文件尾以及增量修订\n- 确定性的完整重写和保留原始字节的增量更新\n- PDF 2.0 创作：页面、内容流、字体、图像、色彩空间、图案、透明度和资源\n- 书签、命名目标、页面标签、链接、附件、批注、可选内容和 AcroForm 字段\n- 带标签的 PDF 以及 PDF/UA-2、PDF/A-4、PDF/A-4e、PDF/A-4f 创作保障\n- RC4、AES-128 和 AES-256 密码安全\n- 分离式 CMS 签名、认证权限、字段锁定、验证以及签名后修订分析\n- 结构诊断、有界解析、实现上限和往返校验\n\n## 项目规模\n\n| 指标 | 当前 1.8.0 版本 |\n| --- | --- |\n| 引擎源文件数 | 151 |\n| C# 物理行数 | 47,725 |\n| 引擎测试数 | 1,436 |\n| 对抗性语料库 PDF 数 | 2,907 |\n\n## 真正的库边界\n\n引擎不依赖 WPF、KillerPDF 应用程序代码、PDFium、PdfPig、PdfSharpCore 或 PDFsharp。它只处理 PDF 字节和类型化的文档概念。渲染、OCR、文本提取、UI 控件和桌面工作流都留在宿主应用程序中。\n\n单一代码库（monorepo）让引擎、应用集成、测试和语料库门禁保持在一个可审查的变更中。同时，项目边界仍然确保它是一个其他 .NET 应用程序可以通过 NuGet 引用的可复用库。\n\n## 设计原则\n\n- 当一个操作可以表示为增量修订时，保留现有字节。\n- 当必需的结构无法被安全地解读或保留时，选择失败拒绝而不是继续。\n- 生成确定性的输出，使回归问题可以复现。\n- 在分配或序列化无上限的结构之前，先施加明确的实现上限。\n- 保持公开 API 类型化，而不是暴露 KillerPDF 应用程序状态。\n- 将标准符合性视为由校验器背书的行为，而不是从 PDF 头部推断出的标签。\n\n## 源代码与许可证\n\n引擎在 [KillerPDF 仓库](https://github.com/SteveTheKiller/KillerPDF/tree/main/engine) 中开发，以 GPLv3 授权。详细的能力演进历史记录在 [引擎更新日志](https://github.com/SteveTheKiller/KillerPDF/blob/main/engine/CHANGELOG.md) 中。\n","getting-started":"# 快速上手\n\n安装 The KillerPDF.Engine，创建一个 PDF 2.0 文档并重新打开它，全程不超过五分钟。\n\n## 环境要求\n\n- .NET 10 SDK\n- 一个 .NET 控制台、桌面、Web 或服务项目\n- 引擎本身支持 Windows、Linux 或 macOS\n\n这个库不含 UI。渲染器、界面、存储和部署模型都由你的应用程序自行选择。\n\n## 从 NuGet 安装\n\n在项目目录中运行：\n\n```powershell\ndotnet add package KillerPdf.Engine\n```\n\n或直接添加包引用：\n\n```xml\n<PackageReference Include=\"KillerPdf.Engine\" Version=\"1.8.0\" />\n```\n\n如果是在 KillerPDF 仓库的检出副本中工作，请改用项目引用：\n\n```xml\n<ProjectReference Include=\"path\\to\\KillerPDF\\engine\\KillerPdf.Engine\\KillerPdf.Engine.csproj\" />\n```\n\n## 创建你的第一个文档\n\n```csharp\nusing KillerPdf.Engine.Authoring;\n\nbyte[] pdf = new PdfDocumentBuilder()\n    .SetMetadata(new PdfDocumentMetadata\n    {\n        Title = \"Hello from The KillerPDF.Engine\",\n        Author = \"Example application\",\n        Language = \"en-US\"\n    })\n    .AddBlankPage(612, 792)\n    .Build();\n\nFile.WriteAllBytes(\"hello.pdf\", pdf);\n```\n\n页面尺寸使用 PDF 点（point）。美式信纸（US Letter）页面为 612 x 792 点。\n\n## 打开并检查它\n\n```csharp\nusing KillerPdf.Engine.Documents;\n\nbyte[] source = File.ReadAllBytes(\"hello.pdf\");\nPdfDocument document = PdfDocument.Open(source);\nPdfDocumentInformation information = PdfDocumentInformation.Read(document);\n\nConsole.WriteLine(information.Title);\nConsole.WriteLine($\"{information.PageCount} page(s)\");\nConsole.WriteLine($\"PDF {information.Version}\");\n```\n\n## 确定性地重写它\n\n```csharp\nusing KillerPdf.Engine.Writing;\n\nbyte[] rewritten = PdfDocumentWriter.Write(document);\nFile.WriteAllBytes(\"hello-rewritten.pdf\", rewritten);\n```\n\n当你需要可复现的输出和规范化的对象图时，使用确定性重写。当需要保留原始字节前缀时，使用增量编辑器。\n\n## 选择下一篇指南\n\n- [读取 PDF](reading-pdfs.html) 介绍元数据、页面、链接、书签和表单。\n- [创建 PDF](creating-pdfs.html) 介绍页面、内容流、元数据和现代标准。\n- [编辑 PDF](editing-pdfs.html) 介绍对既有文件的字节保留式修改。\n- [校验](validation.html) 介绍结构检查和独立校验器。\n","reading-pdfs":"# 读取 PDF\n\n从内存打开文档，检查来历不明的输入，并读取高层文档特性，而不把解析器内部细节暴露给应用程序的其余部分。\n\n## 打开文档\n\n```csharp\nusing KillerPdf.Engine.Documents;\n\nbyte[] source = File.ReadAllBytes(\"input.pdf\");\nPdfDocument document = PdfDocument.Open(source);\n```\n\n对于加密文档，传入用户密码或所有者密码：\n\n```csharp\nPdfDocument document = PdfDocument.Open(source, password);\n```\n\n在 `PdfDocument` 的整个生命周期内，源字节必须保持可用。\n\n## 打开之前先检查\n\n当文件来自陌生或不可信的来源时，使用不抛出异常的结构检查器：\n\n```csharp\nusing KillerPdf.Engine.Diagnostics;\n\nPdfInspectionReport report = PdfDocumentInspector.Inspect(source);\nif (!report.IsStructurallyValid)\n{\n    foreach (PdfDiagnostic diagnostic in report.Diagnostics)\n        Console.WriteLine($\"{diagnostic.Code}: {diagnostic.Message}\");\n}\n```\n\n对加密输入，检查会报告需要身份验证。当应用程序持有密码时，使用 `InspectAuthenticated`。\n\n## 读取文档信息\n\n```csharp\nPdfDocumentInformation information = PdfDocumentInformation.Read(document);\n\nConsole.WriteLine(information.Title);\nConsole.WriteLine(information.Author);\nConsole.WriteLine(information.Language);\nConsole.WriteLine(information.PageCount);\n```\n\n## 读取页面几何信息\n\n```csharp\nIReadOnlyList<PdfPageInformation> pages = PdfPageInformation.Read(document);\n\nfor (int index = 0; index < pages.Count; index++)\n{\n    PdfPageInformation page = pages[index];\n    Console.WriteLine($\"Page {index + 1}: {page.Width} x {page.Height}, {page.Rotation} degrees\");\n}\n```\n\n有效几何信息已经把继承的页面框和旋转考虑在内。\n\n## 读取交互特性\n\n| 特性 | 读取器 |\n| --- | --- |\n| 书签 | `PdfBookmarkReader` |\n| 链接 | `PdfLinkReader` |\n| 表单控件 | `PdfFormWidgetReader` |\n| 签名 | `PdfSignatureReader` |\n\n这些读取器返回稳定的高层记录，其中的文本已解码、几何信息已规范化。对于常见任务，你的应用程序不需要遍历原始字典。\n\n## 读取表单字段\n\n表单控件按页读取。每条结果都包含完全限定的字段名称、字段类型、当前值、标志位、选项以及规范化的页面坐标。\n\n```csharp\nIReadOnlyList<PdfPageInformation> pages = PdfPageInformation.Read(document);\n\nfor (int pageIndex = 0; pageIndex < pages.Count; pageIndex++)\n{\n    foreach (PdfFormWidgetInfo widget in PdfFormWidgetReader.ReadPage(document, pageIndex))\n    {\n        Console.WriteLine($\"{widget.FieldName}: {widget.Value}\");\n        Console.WriteLine($\"  Type: {widget.FieldKind}\");\n        Console.WriteLine($\"  Rectangle: {widget.Left}, {widget.Bottom}, {widget.Right}, {widget.Top}\");\n    }\n}\n```\n\n空白页面返回空列表。页面索引从 0 开始。\n\n## 了解边界\n\n引擎读取的是文档结构。它不渲染页面、不提取页面文本、不提取嵌入图像，也不执行 OCR。请将它与适合你应用程序的渲染器和提取工具搭配使用。\n","creating-pdfs":"# 创建 PDF\n\n使用 `PdfDocumentBuilder` 和 `PdfContentStreamBuilder` 创建类型化的 PDF 2.0 文档，而不必手工拼装原始对象字典。\n\n## 创建页面和元数据\n\n```csharp\nusing KillerPdf.Engine.Authoring;\n\nvar builder = new PdfDocumentBuilder()\n    .SetMetadata(new PdfDocumentMetadata\n    {\n        Title = \"Quarterly report\",\n        Author = \"Example application\",\n        Language = \"en-US\"\n    })\n    .AddBlankPage(612, 792);\n\nbyte[] pdf = builder.Build();\n```\n\n`Build` 会在序列化之前校验相互关联的文档特性。无效的页面几何、不完整的标准结构和不一致的字段定义都会在文件写出之前失败。\n\n## 编写页面内容\n\n```csharp\nvar content = new PdfContentStreamBuilder()\n    .BeginText()\n    .SetFont(PdfStandardFont.Helvetica, 18)\n    .MoveText(72, 720)\n    .ShowLatin1Text(\"Created with The KillerPDF.Engine\")\n    .EndText();\n\nbyte[] pdf = new PdfDocumentBuilder()\n    .AddPage(612, 792, content)\n    .Build();\n```\n\n内容构建器还支持路径、线条样式、颜色、图像、嵌入的 TrueType 字体、透明度、图形状态、图案、渐变、可选内容以及带标签的标记内容。\n\n## 添加文档特性\n\n有些特性本来需要同时修改页面树、目录、资源、名称树、结构树和 AcroForm，同一个构建器会为你统一协调：\n\n- 书签、目标、页面标签和查看器首选项\n- 链接、附件、批注、回复和弹出注释\n- 文本字段、复选框、单选按钮、选择字段、按钮和签名字段\n- 输出意图、嵌入文件、可选内容组和结构元素\n\n请使用 `PdfDocumentBuilder` 上的类型化方法。除非你的应用程序正在实现某个高层 API 尚未开放的 PDF 特性，否则不要下沉到原始对象层。\n\n## 面向 PDF/A 和 PDF/UA 创作\n\n构建器为 PDF/A-4、PDF/A-4e、PDF/A-4f 和 PDF/UA-2 提供协调一致的保障：\n\n```csharp\nvar builder = new PdfDocumentBuilder()\n    .EnablePdfA4Conformance()\n    .EnablePdfUa2Conformance();\n```\n\n仅靠这些调用还不足以达到标准符合性。你的文档仍然需要合适的元数据、输出意图、嵌入字体、语义结构、语言信息和无障碍内容。请用独立校验器校验最终文件。\n\n## 保存结果\n\n```csharp\nFile.WriteAllBytes(\"created.pdf\", builder.Build());\n```\n\n先在内存中构建，再把完成的字节数组提交到你的存储层。这样可以避免一次失败的构建在磁盘上留下残缺的 PDF。\n","editing-pdfs":"# 编辑 PDF\n\n使用增量编辑器修改既有文档：保留其原始字节前缀，并只追加一个有界的修订。\n\n## 为什么增量编辑很重要\n\n增量更新会追加被修改的对象、一个新的交叉引用区段和一个新的文件尾。原始字节保持原样。这对注重保全的工作流、审计追踪以及签名后修订分析非常重要。\n\n当你刻意需要一个规范化的文档时，才使用确定性的完整重写。\n\n## 修改页面\n\n```csharp\nusing KillerPdf.Engine.Documents;\nusing KillerPdf.Engine.Editing;\n\nPdfDocument document = PdfDocument.Open(File.ReadAllBytes(\"input.pdf\"));\n\nbyte[] updated = new PdfIncrementalPageEditor(document)\n    .RotateClockwise(0)\n    .MovePage(2, 0)\n    .AddBlankPage(612, 792)\n    .Build();\n\nFile.WriteAllBytes(\"updated.pdf\", updated);\n```\n\n页面操作包括插入、导入、删除、重新排序、旋转、页面框、用户单位、过渡效果、缩略图、内容替换和内容追加。\n\n## 从另一个 PDF 复制页面\n\n`AddImportedPage` 会把选定的页面及其依赖对象图复制到目标文档中。如果任一文档包含 AcroForm，编辑器还会合并表单字段树、重命名 `/DR` 中冲突的资源名称，并改写受影响的 `/DA` 字符串。\n\n```csharp\nbyte[] destinationBytes = File.ReadAllBytes(\"destination.pdf\");\nbyte[] sourceBytes = File.ReadAllBytes(\"source.pdf\");\n\nPdfDocument destination = PdfDocument.Open(destinationBytes);\nPdfDocument source = PdfDocument.Open(sourceBytes);\n\nbyte[] updated = new PdfIncrementalPageEditor(destination)\n    .AddImportedPage(source, sourcePageIndex: 0)\n    .Build();\n\nFile.WriteAllBytes(\"combined.pdf\", updated);\n```\n\n页面索引从 0 开始。完全限定的表单字段名称重复时会抛出异常，而不会被自动重命名。\n\n## 在页面指定位置插入图像\n\n使用 `PdfContentStreamBuilder` 把图像直接放入页面内容中。PDF 坐标以页面左下角为原点。\n\n```csharp\nusing KillerPdf.Engine.Authoring;\n\nPdfPageInformation page = PdfPageInformation.Read(document)[0];\nPdfImage image = PdfImage.FromJpeg(File.ReadAllBytes(\"photo.jpg\"));\n\nvar content = new PdfContentStreamBuilder()\n    .DrawImage(image, x: 72, y: 500, width: 180, height: 120);\n\nbyte[] updated = new PdfIncrementalPageEditor(document)\n    .AppendPageContent(0, page.Width, page.Height, content)\n    .Build();\n\nFile.WriteAllBytes(\"with-image.pdf\", updated);\n```\n\n如果图像应保持为批注，请使用 `PdfIncrementalAnnotationEditor.AddImageStamp`，并指定相同的页面、位置和尺寸。\n\n## 更新文档特性\n\n`PdfIncrementalPageEditor` 还可以修改元数据、输出意图、页面布局、查看器首选项、打开动作、命名目标、页面标签、附件、书签和 AcroForm 字段。\n\n```csharp\nbyte[] updated = new PdfIncrementalPageEditor(document)\n    .SetTextFieldValue(\"Customer.Name\", \"Ada Lovelace\")\n    .SetCheckBoxValue(\"Terms.Accepted\", true)\n    .Build();\n```\n\n## 编辑批注\n\n对页面批注和链接，使用 `PdfIncrementalAnnotationEditor`：\n\n```csharp\nvar editor = new PdfIncrementalAnnotationEditor(document);\n\nbyte[] updated = editor\n    .AddTextNote(0, 72, 700, \"Reviewed\")\n    .Build();\n```\n\n批注编辑器支持文本注释、文本标记、自由文本、直线、形状、墨迹、图像图章、密文标记、文件附件和链接。\n\n## 安全地导入\n\n页面导入会复制完整的依赖对象图，并处理命名冲突。当导入依赖不受支持的全局状态时，编辑器会拒绝执行，而不是产出一个表单、导航、可选内容或无障碍结构被悄悄破坏的文档。\n\n## 写入新的目标位置\n\n在 `Build` 成功之前，请保持源路径与输出路径分离。只有当你的应用程序完成了自己的校验和可靠写入流程之后，才替换原始文件。\n","validation":"# 校验\n\n校验是引擎设计的一部分，而不是写出之后再贴上的标签。编辑之前使用结构检查，确定性重写使用往返校验，标准符合性声明则交给独立工具。\n\n## 检查来历不明的输入\n\n```csharp\nusing KillerPdf.Engine.Diagnostics;\n\nPdfInspectionReport report = PdfDocumentInspector.Inspect(source);\n\nif (!report.IsStructurallyValid)\n{\n    foreach (PdfDiagnostic diagnostic in report.Diagnostics)\n        Console.WriteLine($\"{diagnostic.Severity}: {diagnostic.Message}\");\n}\n```\n\n检查是有界的，并且对结构性失败不抛出异常。它会检查文件头、修订链、交叉引用数据、间接对象、目录、对象上限和身份验证状态。\n\n## 校验确定性重写\n\n```csharp\nusing KillerPdf.Engine.Validation;\n\nPdfRoundTripResult result = PdfRoundTripValidator.Validate(source);\nif (!result.Succeeded)\n    throw new InvalidOperationException(result.FailureMessage);\n\nFile.WriteAllBytes(\"validated.pdf\", result.RewrittenBytes!);\n```\n\n往返校验器会检查源文件、重写它、重新打开结果、再次写出，并比较两次输出。对受密码保护的输入，使用 `ValidateAuthenticated`。\n\n## 理解这能证明什么\n\n| 检查 | 它证明了什么 |\n| --- | --- |\n| 结构检查 | 必需的 PDF 结构可以在明确的上限内被解析 |\n| 写出后重新打开 | 引擎能够解析它自己产出的输出 |\n| 第二次确定性写出 | 等价的输入产生完全相同的规范化输出 |\n| 独立校验 | 另一个独立实现接受所声明的结构或配置文件 |\n\n> 引擎往返校验成功并不能替代 veraPDF、qpdf、签名验证工具或针对应用程序的验收测试。\n\n## 独立的发布门禁\n\n引擎的发布流程组合了以下环节：\n\n- 1,436 个单元测试和回归测试\n- 零警告的严格 Release 构建\n- 2,907 个文件的增量结构语料库门禁\n- 2,907 个文件的选定页面导入语料库门禁\n- qpdf 结构检查\n- veraPDF 的 PDF/A-4 和 PDF/UA-2 校验\n- 使用 OpenSSL 验证真实的分离式 CMS 签名样本\n\n许多语料库文件是畸形的、加密的或刻意不符合标准的。当源文件无法被安全编辑时，一次有记录的拒绝是预期结果。发布门禁会把这类边界与意外失败区分开。\n\n## 校验你声明的配置文件\n\n对 PDF/A 和 PDF/UA 配置文件使用 veraPDF，对一般结构检查使用 qpdf，对签名互操作性则使用独立于引擎的密码学实现。请在构建或发布记录中保留所用校验器的确切版本和配置文件。\n","signing-encryption":"# 签名与加密\n\n引擎支持密码安全、分离式 CMS 签名、认证策略、字段锁定、签名发现、密码学验证以及签名后修订分析。\n\n## 创建受密码保护的文档\n\n```csharp\nusing KillerPdf.Engine.Authoring;\nusing KillerPdf.Engine.Security;\n\nvar encryption = new PdfPasswordEncryptionOptions\n{\n    UserPassword = \"reader password\",\n    OwnerPassword = \"owner password\",\n    AllowContentCopying = false,\n    AllowDocumentModification = false,\n    AllowHighQualityPrinting = true\n};\n\nbyte[] protectedPdf = new PdfDocumentBuilder()\n    .SetPasswordEncryption(encryption)\n    .AddBlankPage()\n    .Build();\n```\n\n新文档使用 PDF 2.0 的 AES-256 保护。引擎也能读取受支持的 RC4 和 AES-128 文档，并在增量更新和重写过程中保留已通过身份验证的安全设置。\n\n## 打开加密输入\n\n```csharp\nusing KillerPdf.Engine.Documents;\n\nPdfDocument document = PdfDocument.Open(source, password);\nConsole.WriteLine(document.PasswordAuthenticationRole);\nConsole.WriteLine(document.DeclaredPermissions?.AllowContentCopying);\n```\n\n请在应用程序工作流中尊重通过身份验证得到的权限集。所有者密码授予不受限制的访问权。用户密码可能会限制打印、复制、页面组装、表单、批注或一般性修改。\n\n## 使用分离式 CMS 签名\n\n`PdfDetachedSignatureWriter` 负责准备 PDF 签名修订，并回调你的应用程序来生成分离式 CMS 值。证书选择、私钥访问、时间戳服务策略和信任配置都由你的应用程序掌控。\n\n```csharp\nusing KillerPdf.Engine.Signing;\n\nvar options = new PdfSignatureOptions\n{\n    FieldName = \"Signature1\",\n    SignerName = \"Example signer\",\n    Reason = \"Approved\",\n    DigestMethod = PdfSignatureDigestMethod.Sha256\n};\n```\n\n请使用与你的证书和 CMS 提供程序相匹配的 `Sign` 重载。不要把私钥复制进 PDF 或应用程序日志。\n\n## 读取并验证签名\n\n```csharp\nIReadOnlyList<PdfSignatureInfo> signatures = PdfSignatureReader.Read(document);\n\nforeach (PdfSignatureInfo signature in signatures)\n{\n    PdfSignatureVerificationResult integrity =\n        PdfSignatureVerifier.VerifyIntegrity(document, signature);\n    Console.WriteLine(integrity.IsCryptographicallyValid);\n}\n```\n\n完整性验证回答的是：被签名的字节是否与签名相符。信任验证则在此之上，依据你的应用程序提供的信任策略评估证书链。\n\n## 分析后续修订\n\n`PdfSignedRevisionAnalyzer` 能识别签名之后发生的更改，并依据认证权限和字段锁定对其进行评估。这不同于简单的密码学通过或失败结果。一个签名可以在密码学上保持完好，而后续修订加入的更改却违反了已声明的策略。\n\n## 安全边界\n\n- 将 PDF 密码视为敏感数据，不要保留超过必要的时间。\n- 把私钥放在操作系统证书存储、硬件或其他受保护的签名提供程序中。\n- 让信任策略保持明确。不要默认信任任何嵌入的证书。\n- 发布之前，用独立的 CMS 实现验证签名输出。\n","api":"# API 指南\n\n公开 API 按 PDF 职责划分。请从高层的文档、创作、编辑和校验类型入手。只有当你刻意需要更底层的 PDF 层时，才使用语法和对象类型。\n\n## 主要命名空间\n\n| 命名空间 | 用途 |\n| --- | --- |\n| `KillerPdf.Engine.Documents` | 打开文件，读取文档、页面、书签、链接、表单和树信息 |\n| `KillerPdf.Engine.Authoring` | 创建文档、页面内容、资源、批注、表单、导航和标准符合性结构 |\n| `KillerPdf.Engine.Editing` | 向既有文档追加保留字节的修订 |\n| `KillerPdf.Engine.Writing` | 确定性重写、增量更新选项和输出净化 |\n| `KillerPdf.Engine.Diagnostics` | 有界的结构检查与诊断 |\n| `KillerPdf.Engine.Validation` | 确定性重新打开和往返验证 |\n| `KillerPdf.Engine.Security` | 密码身份验证、权限、加密选项和安全处理器 |\n| `KillerPdf.Engine.Signing` | 签名创建、发现、验证、认证和修订分析 |\n| `KillerPdf.Engine.Objects` | 面向底层工作的类型化 PDF 对象 |\n| `KillerPdf.Engine.Syntax` | 文件头、标记、词法分析器行为和 PDF 版本 |\n\n## 常用入口\n\n| 任务 | 从这里开始 |\n| --- | --- |\n| 打开 PDF | `PdfDocument.Open` |\n| 检查未知输入 | `PdfDocumentInspector.Inspect` |\n| 读取元数据和页数 | `PdfDocumentInformation.Read` |\n| 读取页面尺寸和旋转 | `PdfPageInformation.Read` |\n| 读取书签 | `PdfBookmarkReader` |\n| 读取链接 | `PdfLinkReader` |\n| 读取表单 | `PdfFormWidgetReader` |\n| 创建 PDF | `PdfDocumentBuilder` |\n| 构建页面内容 | `PdfContentStreamBuilder` |\n| 修改页面和表单 | `PdfIncrementalPageEditor` |\n| 修改批注和链接 | `PdfIncrementalAnnotationEditor` |\n| 确定性重写 | `PdfDocumentWriter.Write` |\n| 校验重写结果 | `PdfRoundTripValidator.Validate` |\n| 读取并验证签名 | `PdfSignatureReader` 和 `PdfSignatureVerifier` |\n\n## 先高层，后底层\n\n高层 API 会协调跨多个相关 PDF 结构的更改。例如，添加一个表单字段可能会影响页面批注、AcroForm 字段树、外观资源、默认资源和文档目录。请优先使用能表达你意图的类型化方法。\n\n当要实现高层 API 尚未覆盖的特性时，再使用 `PdfObject`、`PdfDictionary`、`PdfArray`、`PdfStream`、`PdfName` 和间接引用。写出之前请校验完整的对象图。\n\n## 在 IDE 中查看 API 文档\n\n软件包附带由公开源代码注释生成的 XML 文档。Visual Studio、Rider 以及其他 .NET 编辑器会在你输入时显示摘要和参数帮助。\n\n要了解当前完整的 API 表面，请浏览 [引擎源代码](https://github.com/SteveTheKiller/KillerPDF/tree/main/engine/KillerPdf.Engine)，或用 IDE 的对象浏览器查看软件包。\n\n## 版本策略\n\n软件包版本跟随附带它的 KillerPDF 发布版本。请锁定你的应用程序已测试过的版本。升级之前，请查阅 [引擎更新日志](https://github.com/SteveTheKiller/KillerPDF/blob/main/engine/CHANGELOG.md)。\n","architecture":"# 架构\n\nThe KillerPDF.Engine 是 KillerPDF 单一代码库（monorepo）中的一个可复用文档库。项目引用强制维持一条不含 UI 的边界，而共享仓库让应用集成、库行为、测试和语料库门禁能够一起演进。\n\n## 库边界\n\n引擎负责：\n\n- PDF 语法和对象图\n- 文档加载和对象的惰性解析\n- 创作与结构化编辑\n- 表单、批注、导航和附件\n- 加密、签名以及签名后修订分析\n- PDF 2.0、PDF/A、PDF/UA、诊断和校验\n\n宿主应用程序负责：\n\n- 渲染和显示界面\n- OCR 和页面文本提取\n- 用户界面和工作流\n- 打印、文件选择器、设置以及操作系统集成\n\n引擎不引用 WPF、KillerPDF 应用程序代码、PDFium、PdfPig、PdfSharpCore 或 PDFsharp。\n\n## 内部分层\n\n| 层 | 职责 |\n| --- | --- |\n| `Syntax` 和 `Objects` | 二进制安全的标记和类型化 PDF 对象模型 |\n| `CrossReference`、`Filters` 和 `Documents` | 修订发现、流解码、惰性解析和经过身份验证的访问 |\n| `Authoring` | 页面、内容、资源、元数据、交互特性和标准结构的类型化构建 |\n| `Editing` | 高层的字节保留式修改和安全的跨文档对象图导入 |\n| `Writing` | 增量修订、确定性重写、元数据策略和输出净化 |\n| `Security` 和 `Signing` | 身份验证、权限、加密、签名和修订验证 |\n| `Diagnostics` 和 `Validation` | 有界检查、实现上限、结构报告和往返检查 |\n\n## 读取流程\n\n1. 解析文件头，定位最后一个 `startxref` 标记。\n2. 读取交叉引用修订链。\n3. 在解析受保护对象之前，先对加密文档进行身份验证。\n4. 通过交叉引用表惰性解析间接对象。\n5. 通过类型化读取器呈现常用文档特性。\n\n## 写出流程\n\n对于确定性重写，`PdfDocumentWriter` 会遍历已解析的对象图并序列化出一个规范化文档。用等价输入重复写出会得到可复现的输出。\n\n对于增量编辑，高层编辑器会记录预期的更改，在需要时导入依赖的对象图，校验结果，然后在不改动原始字节前缀的情况下追加一个新修订。\n\n## 失败即拒绝的编辑\n\n有些操作会刻意拒绝不完整的全局结构。例如：从带标签的文档进行不安全的部分导入、有歧义的共享对象图、不受支持的 XFA 合并、缺少凭据，以及被已验证的用户权限所禁止的编辑。\n\n拒绝执行，比产出一个能打开但导航、表单、无障碍、可选内容或签名已被悄悄破坏的文件更安全。\n\n## 架构决策\n\n- [ADR-001：PDF 引擎边界](https://github.com/SteveTheKiller/KillerPDF/blob/main/engine/docs/architecture/ADR-001-pdf-engine-boundary.md)\n- [ADR-002：现代 Windows 外壳集成](https://github.com/SteveTheKiller/KillerPDF/blob/main/engine/docs/architecture/ADR-002-integrate-engine-through-modern-windows-shell.md)\n- [ADR-003：安装版与便携版软件包](https://github.com/SteveTheKiller/KillerPDF/blob/main/engine/docs/architecture/ADR-003-split-installed-and-portable-packages.md)\n"}
};})();
